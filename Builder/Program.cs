using Builder.Builders;
using Builder.Models;

namespace Builder
{
	internal class Program
	{
		static void Main(string[] args)
		{
			#region Player 

			//var archerBuilder = new ArcherBuilder();
			//var warriorBuilder = new WarriorBilder();
			//var berserqueBuilder = new BerserqueBuilder();
			//var bumBuilder = new BumBuilder();

			//PlayerBuilder playerBuilder = archerBuilder;
			//HeroCreator.CreateHero(playerBuilder);
			//playerBuilder.Player.GetInfo();

			//playerBuilder = warriorBuilder;
			//HeroCreator.CreateHero(playerBuilder);
			//playerBuilder.Player.GetInfo();

			//playerBuilder = berserqueBuilder;
			//HeroCreator.CreateHero(playerBuilder);
			//playerBuilder.Player.GetInfo();

			//playerBuilder = bumBuilder;
			//HeroCreator.CreateHero(playerBuilder);
			//playerBuilder.Player.GetInfo();

			#endregion

			#region Car 

			//CarBuilder builder = new DaewooLanos();
			//CarCreator.CreateCar(builder);
			//builder.Car.GetInfo();

			//builder = new FordProbe();
			//CarCreator.CreateCar(builder);
			//builder.Car.GetInfo();


			#endregion
		}
	}
}